<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Mapper.UserMapper">

<!--    查询功能    -->

    <select id="getUserPasswordByName" resultType="string">
        select password from users where name=#{userName}
    </select>

    <select id="getUserIdByName" resultType="integer">
        select userId from users where name=#{userName}
    </select>


    <select id="getUserNameByName" resultType="string">
        select name from users where name=#{userName}
    </select>
    <select id="getUserEmailByName" resultType="string">
        select email from users where name=#{userName}
    </select>
    <select id="getUserPhoneByName" resultType="string">
        select phone from users where name=#{userName}
    </select>

    <select id="getUserEmailByEmail" resultType="string">
        select email from users where email=#{userEmail}
    </select>

    <select id="getUserPhoneByPhone" resultType="string">
        select phone from users where phone=#{userPhone}
    </select>


<!--    注册功能    -->

    <insert id="addUser">
        insert into users (name, password, email, phone) values (#{userName}, #{userPassword}, #{userEmail}, #{userPhone})
    </insert>

    <select id="getUsers" resultType="com.Entity.User">
        select userId as userId, name as userName, password as userPassword,
        email as userEmail, phone as userPhone
        from users
    </select>



<!--    修改功能    -->
    <update id="updateUserPasswordByName">
        update users set password = #{newPassword} where name = #{userName}
    </update>

</mapper>